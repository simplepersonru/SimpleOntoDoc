@using SimpleOntoDoc
@model MarkdownIndexViewModel
@{
    Layout = null;
}

# Описание

@Model.Description

# Статистика

| Наименование | Количество |
|--------------|------------|
| Классы       | @Model.ClassCount |
| Перечисления | @Model.EnumCount |
| Примитивы    | @Model.PrimitiveCount |
| Типы данных  | @Model.DataTypeCount |
| Составные типы | @Model.CompoundCount |

@if (!string.IsNullOrEmpty(Model.AllClassesDiagramContent))
{
<text>
# Полная диаграмма классов

```plantuml
@Model.AllClassesDiagramContent
```
</text>
}

# Классы

## Легенда
@if (Model.ClassCount > 0)
{
@:🟦 - класс  
}
@if (Model.EnumCount > 0)
{
@:🟪 - перечисление  
}
@if (Model.PrimitiveCount > 0)
{
@:🟧 - примитив  
}
@if (Model.DataTypeCount > 0)
{
@:🟨 - тип данных  
}
@if (Model.CompoundCount > 0)
{
@:🟥 - составной тип  
}

| Идентификатор | Наименование | Описание |
|---------------|--------------|----------|
@foreach (var cls in Model.Classes)
{
@:| @cls.MarkdownColorSquare() @cls.MarkdownRef(near: false) | @cls.Display | @cls.Description |
}

Сделано с помощью [SimpleOntoDoc](https://github.com/simplepersonru/SimpleOntoDoc)      
