@using SimpleOntoDoc
@model MarkdownIndexViewModel
@{
    Layout = null;
}

# Описание

@Model.Description

# Статистика

| Наименование | Количество |
|--------------|------------|
| Классы       | @Model.ClassCount |
| Перечисления | @Model.EnumCount |
| Примитивы    | @Model.PrimitiveCount |
| Типы данных  | @Model.DataTypeCount |

# Классы

## Легенда
@if (Model.ClassCount > 0)
{
@:🟦 - класс  
}
@if (Model.EnumCount > 0)
{
@:🟪 - перечисление  
}
@if (Model.PrimitiveCount > 0)
{
@:🟧 - примитив  
}
@if (Model.DataTypeCount > 0)
{
@:🟨 - тип данных  
}

| Идентификатор | Описание |
|---------------|----------|
@foreach (var cls in Model.Classes)
{
@:| @cls.MarkdownColorSquare() @cls.MarkdownRef(near: false) | @cls.Description |
}

